<div class="form-wrapper">
  <%= simple_form_for([@prison, @inmate], html: { class: 'form-inline' }) do |f| %>
    <div class="form-group">
      <%= f.input :name %>
    </div>
    <%= f.input :image_url %>
    <%= f.submit "Add", class: "btn btn-warning" %>
  <% end %>
</div>
